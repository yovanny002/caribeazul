<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Contrato Hipotecario #<%= contrato.id %></title>

    <link
        href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
        rel="stylesheet">

    <style>
        /* Estilo profesional para lectura */

        .contrato-wrapper {
            background: white;
            padding: 2rem;
            border-radius: .7rem;
            border: 1px solid #e9ecef;
            box-shadow: 0 4px 15px rgba(0,0,0,0.05);
        }

        .contrato-title {
            font-weight: 700;
            margin-bottom: 1.5rem;
            color: #0d6efd;
        }

        .contrato-content {
            white-space: pre-wrap;
            font-family: 'Times New Roman', serif;
            font-size: 16px;
            line-height: 1.65;
            text-align: justify;
        }

        /* Impresión */
        @media print {
            body {
                margin: 0;
                padding: 0;
            }

            .contrato-wrapper {
                box-shadow: none;
                border: none;
                padding: 1in;
                width: 8.5in;
                height: 13in; /* Tamaño legal */
            }

            .no-print {
                display: none !important;
            }
        }
    </style>
</head>

<body class="bg-light">

    <div class="container py-4">

        <!-- Encabezado -->
        <div class="d-flex justify-content-between align-items-center mb-4 no-print">
            <h2 class="fw-bold">Contrato Hipotecario #<%= contrato.id %></h2>

            <div>
                <a href="/hipotecas/<%= contrato.id %>/download" class="btn btn-primary me-2">
                    <i class="fas fa-download"></i> Descargar
                </a>

                <a href="/hipotecas/<%= contrato.id %>/print" target="_blank" class="btn btn-secondary">
                    <i class="fas fa-print"></i> Imprimir
                </a>
            </div>
        </div>

        <!-- Contenido del contrato -->
        <div class="contrato-wrapper">
            <div class="contrato-content">
                <%- contrato.contrato_texto %>
            </div>
        </div>

        <a href="/hipotecas" class="btn btn-dark mt-4 no-print">
            <i class="fas fa-arrow-left"></i> Volver a la lista
        </a>
    </div>

    <script src="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/js/all.min.js"></script>

</body>
</html>
